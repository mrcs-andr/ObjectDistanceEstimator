<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/action_view_intrinsics"
        android:title="@string/view_intrinsics"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_intrinsics_calibration"
        android:title="@string/calibration"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_extrinsics_calibration"
        android:title="@string/extrinsics_calibration"
        app:showAsAction="never" />
</menu>
